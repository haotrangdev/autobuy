# ══════════════════════════════════════════════════════════════════
#  .env.example – AutoBuy Bot
#  Copy file này thành .env và điền giá trị thực
#  File .env KHÔNG được commit lên git
# ══════════════════════════════════════════════════════════════════

# ── UI Mode ───────────────────────────────────────────────────────
# web | terminal | electron (mặc định: web)
UI_MODE=web

# ── Web UI ────────────────────────────────────────────────────────
# Port cho Web UI (mặc định lấy từ sites config)
# WEB_PORT=3000

# Bật remote access (0.0.0.0 thay vì 127.0.0.1)
# Khi bật nhớ set password trong auth.json
# REMOTE=false

# ── Debug ─────────────────────────────────────────────────────────
# In thêm thông tin debug (puppeteer, network, ...)
# DEBUG=false

# ── Puppeteer ─────────────────────────────────────────────────────
# Đường dẫn Chrome/Chromium tùy chỉnh (để trống = dùng bundled)
# PUPPETEER_EXECUTABLE_PATH=

# Headless mode: true | false | new (default: new)
# PUPPETEER_HEADLESS=new
