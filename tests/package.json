{
  "name": "autobuy-tests",
  "version": "1.0.0",
  "description": "Test suite for AutoBuy bot",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js",
    "test:verbose": "node --test --reporter=spec tests/*.test.js"
  },
  "engines": { "node": ">=18" }
}
